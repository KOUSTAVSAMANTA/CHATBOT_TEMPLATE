import React from 'react'

const Payment = () => {
    const handleSend = async (amount)=>{
        // console.log(e.key)
            
            console.log("hi")
    }
    
  return (
    <div>
        <button onClick={handleSend}>hello</button>
      
    </div>
  )
}

export default Payment
